// var cbt_tunnels = require('./cbt_tunnels.js')
var cbt_tunnels = require('./tunnel_start.js')

console.dir(cbt_tunnels)
let tun = cbt_tunnels.start({username: 'johnr@crossbrowsertesting.com', authkey: 'sandbox', nokill: true, verbose: true}, (err, data) =>{
    console.log(err, data)
})

// setInterval(() => {console.log('still alive!')}, 1000)
