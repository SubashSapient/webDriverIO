req = require('request')

req.get('https://crossbrowsertesting.com', (err, res, body) => {
    if ( err ) (console.error(err))
    console.log(body.length);
})

req.get('http://example.com', (err, res, body) => {
    if ( err ) (console.error(err))
    console.log(body.length);
})
